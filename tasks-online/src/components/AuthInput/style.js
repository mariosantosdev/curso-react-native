import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    icon: {
        color: '#fff',
        marginLeft: 10
    },
    input: {
        marginLeft: 20,
        color: '#fff'
    },
});

export default styles